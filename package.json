{
  "name": "careercraft",
  "version": "1.0.0",
  "scripts": {
    "vercel-build": "rm -rf frontend/node_modules && cd frontend && npm install && npm run build && cd .. && rm -rf public && mv frontend/dist public"
  }
}
