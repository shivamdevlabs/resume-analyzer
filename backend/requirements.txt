# FastAPI core
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Settings & env
pydantic-settings==2.3.1
python-dotenv==1.0.1

# MongoDB (async)
motor==3.4.0
pymongo==4.7.2

# Resume file parsing
pypdf==4.2.0
python-docx==1.1.2

# HTTP client — used to call Gemini REST API directly
# (replaces google-generativeai SDK which is incompatible with Python 3.14)
httpx==0.27.0

# PDF generation
reportlab==4.2.2
